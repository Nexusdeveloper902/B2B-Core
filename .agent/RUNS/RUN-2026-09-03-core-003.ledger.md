# RUN-2026-09-03-core-003 — activity ledger

| Time (approx) | Activity | Evidence / result |
|---|---|---|
| start | Read worklog + .agent memory; confirmed repo reality (TASK-003 complete at ce601b8) | worklog.md, git log |
| +10m | Phase A: API forensics on the remote — workflow registered under its PATH with 0 runs and dispatch 422 while blob shas matched → file valid YAML locally → must be a compile-level defect; task record + ADR-012 + OBS-005 drafted | TASK-004 record |
| +20m | Phase B: found the two compile killers (colon in unquoted job name = YAML syntax error; `secrets` in job-level `if`); rewrote ci.yml (gate-job pattern, workflows-lint/actionlint job, permissions/concurrency/timeouts); GEMINI_API_KEY stored as repo secret via libsodium sealed-box API call | commit 074c531 |
| +30m | Phase C: local actionlint (later re-run WITH shellcheck after SC2129 slipped through), YAML parse, `./run quality`; push; dispatch → **run 1: 14 jobs, 8 pass** — first runs ever in the Actions tab | run 33703136356 |
| +45m | Phase D-1: log-driven fixes — gitleaks allowlist for the deliberate 401-test token, drop PHP 8.3 matrix legs (lock floor 8.4.1), remove `--live-llm` phantom flag, Arch `php-sqlite` split discovery (package contents verified from the mirror tarball), setup-before-doctor reordering; **run 2: 9/12; live-LLM smoke PASSED from US runners** | run 33704172723 |
| +70m | Phase D-2: PR-event trigger test (negative — OBS-006: sandbox pushes/PR events emit nothing; dispatch is the automation path); always-on PHP diagnostics step in arch job → exposed the double-prefix sed bug (`extension=\1` where group 1 already contains `extension=`) + the `printf %b` SOH corruption of printed remediation; backreference-free sed written and verified against the extracted Arch php.ini; ScriptSuiteTest guard updated; botched branch state fixed via cherry-pick (run 3 cancelled as stale) | run 4: 11/12 |
| +90m | Phase D-3: composer platform check needs ext-iconv on Arch (module #16 everywhere, parity kept); safe.directory for manual clones | run 5: 11/12, Arch reached `./run test` (100/109 on PHP 8.5.10) |
| +100m | Phase D-4: remaining 8 Arch failures need ext-gd; Arch ships gd.so only in `php-gd` (verified from package + API) → pacman line `php php-sqlite php-gd composer`; gd = module #17 | commit b0fd17d |
| +110m | **Run 6: 12/12 success** — all jobs green at the tip of main; badge "CI - passing"; workflow "CI"/active; 6 runs visible in the Actions tab | run 33705375607 |
| end | Phase E: run record, ledger, state snapshot, PROJECT.md facts, TASK-004 status, worklog update; final push + verification | this record |

Tool failures encountered: PR creation failed once (branch had no commits
over main — fixed with a verification commit); one push landed on the
feature branch by mistake (cherry-picked to main, stale run cancelled);
hermetic job initially ran doctor before setup (reordered). No secret ever
entered git history (verified by scan of every commit).
