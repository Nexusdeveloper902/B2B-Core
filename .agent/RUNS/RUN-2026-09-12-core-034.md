# RUN-2026-09-12-core-034 — TASK-034: confirms, readers fit, coupling, live search

Owner report (chat, 2026-09-12): native confirms still shown (build
them); readers right panel cramped (stacked buttons, no spacing);
grade 4 should offer 4° A/B auto; searches should feel realtime.

## Changes
- `components/confirm-modal.blade.php` (new) + `.modal-*` /
  `.btn-danger` / `.row-actions` / ledger-input CSS; lang
  `modal_cancel` EN+ES; readers + pairing wired (confirm flows
  extracted, behavior identical).
- Readers section even-split; `row-actions` on Blade + JS-built rows.
- Students desk: `data-grade` options, `syncClassesToGrade`
  (change/load/reset/create/rename paths), live `fetchRoster`
  search + `#roster-pages` swap + pagination delegation.
- Tests: +2 AdminDesksTest pins (modal-never-native,
  coupling+live-search markers).

## Measurements (local serve + real browser, admin session)
- Readers table 638px in 507px panel → after fix 608/608, 0 overflow.
- Modal message/actions gap 0px → 16px (specificity-tie fix).
- Grade 4° → visible {4° A, 4° B}, auto 4° A (20/22 hidden).
- "carlos" typed → `?q=carlos`, one row, zero reloads.
- 390px: stacked cards, full-width gapped buttons.
- Unpair modal opens with per-card copy, Esc closes (no destructive
  click during verification).

## Gates
- `./run test` **445 passed / 3 skipped** (was 443/3; +2 new pins)
- `./run quality` PASS (Pint incl. new component-adjacent tests,
  BilingualJourney lang parity with `modal_cancel`)
- Artifacts removed (screenshots), server stopped via PID (no
  `pkill -f` self-match this time).

## Status
DONE 2026-09-12 — uncommitted tree; commit/push per owner call.
