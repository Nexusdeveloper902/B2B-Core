# RUN-2026-09-06-core-020 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection

- Fresh clones: B2B-Marketplace, B2B-Core, B2B-Firmware (PAT via
  env-var credential helper — token never in remote URLs/config;
  remotes verified clean after clone)
- Read .agent memory: PROJECT.md, TASK-006/007/010, ADR-003/005/006/
  007/012/015/016/019, OBS-002/007/008/012, STATE core-019
- Read code: GeminiClient, GeminiClassifier, ClassifierFactory,
  NlQueryService/FunctionRegistry/NlQueryException, NlQueryController,
  AppServiceProvider, config/recycling.php, .env.example, phpunit.xml,
  ci.yml, llm-check/status/test/e2e scripts, run dispatcher, all
  Gemini-touching tests, README/API/LOCAL_MODEL/SCRIPTS docs EN+ES,
  postman collection, lang files EN+ES
- Web research (page_reader + web_search): 9 DeepSeek doc pages →
  /home/z/my-project/scripts/ds_*.txt (sandbox artifacts, not repo
  files)

## State-modifying commands (dev sandbox)

- `./run toolchain` → static PHP 8.4.23 + Composer 2.10.3 into
  .tools/ (gitignored)
- `./run setup` (composer install, .env from .env.example, migrate,
  seed demo)
- Baseline `./run test` (green: 235/1/3564) BEFORE first edit
- Edits: 5 new files (DeepSeekClient, DeepSeekClassifier,
  DeepSeekClientTest, TASK-022, ADR-030, OBS-013) + RUN/SNAPSHOT/
  ledger/PROJECT append + ~30 modified files + 3 git-rm deletions
  (GeminiClient, GeminiClassifier, GeminiClientTest)
- Local .env (gitignored) patched GEMINI_* → DEEPSEEK_* for honest
  llm-check behavior in this sandbox
- shellcheck 0.10.0 downloaded to /home/z/my-project/tools/shellcheck
  (sandbox artifact — same version CI installs; not a repo file)
- Gates: quality (Pint auto-fix first), test, e2e, llm-check — all
  PASS/exit-2-by-design as recorded in the RUN file

## Push record

- Branch feature/TASK-022-deepseek-provider-migration → commit →
  merge --no-ff to main → push (one-time credential-helper env var;
  token never persisted)
- CI: workflow_dispatch dispatched via REST after push (OBS-006:
  sandbox pushes may emit no events); run id + result to be recorded
  in the worklog; the live-llm-smoke job gates OFF until the owner
  adds the DEEPSEEK_API_KEY secret (honest skip by design)
