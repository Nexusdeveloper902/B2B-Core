# RUN-2026-09-11-core-031 — TASK-031: DeepSeek Flash lineup + vision re-verification

## Context
Owner (chat, 2026-09-11): CV is being remade for DeepSeek — re-check
the docs, re-check the implementation. Branch
`feature/TASK-031-deepseek-flash-model-lineup` (main untouched).
Docs re-pulled same day: vision, thinking_mode, json_mode, pricing,
error_codes (OBS-016).

## Docs verdict (the headline)
The lineup moved since TASK-022: the model is **`deepseek-flash`**
(V4.1-Flash, vision-capable). Legacy `deepseek-v4-flash` +
`deepseek-v4-flash-vision-exp` still accepted, retired, served+billed
as Flash. Nothing broken today — every default named a retired ID.
Wire reconfirmed current, unchanged: thinking toggle shape, temp-0
validity, JSON contract, user-only image parts, error taxonomy.

## Changes
- Defaults → `deepseek-flash`: DeepSeekClient, DeepSeekClassifier,
  config (both), AppServiceProvider fallback, ClassifierFactory
  fallback (was missing), llm-check (defaults + retired-vs-dead
  warnings, bilingual), .env.example, lang model-not-found EN+ES,
  README/API/LOCAL_MODEL EN+ES (+new deepseek-fallback subsection).
- Vision hardening: `detail: high` pinned, local pre-flight (finfo
  content-detect JPEG/PNG/GIF/WebP, injectable 32 MiB ceiling,
  single-read no-TOCTOU), explicit empty-content diagnosis,
  strictBool (stringy "false" trap closed), error.message in failure
  summary.
- First-ever Http::fake cover for the vision driver (was live-only):
  DeepSeekClassifierTest (18) + chat default-model pin; test fixtures
  migrated to canonical (one legacy-compat pin kept);
  DocumentationTest `deepseek-flash` needles EN+ES.
- ADR-046 (supersedes ADR-030 model choice), OBS-016, TASK-031.

## Verification
- Focused: 40/1-skip green. Full: **442/3 (8,211 assertions)** +
  quality PASS + e2e 33/33.
- Adversarial audit SHIP-WITH-FIXES — all honored: fixture IDs (MAJOR),
  finfo-false TypeError (MAJOR), missing fake (MAJOR), TOCTOU/message
  (MINOR), factory fallback (MINOR), stringy bools (MINOR, then caught
  a REAL filter_var(null) trap via failing test), error.message
  (MINOR), composer ext-fileinfo consciously NOT added (lock-hash
  churn; finfo-false path handled + CI provisions it — documented).
- Kept per auditor verdicts: detail high, 404 mapping.

## Status
DONE on the branch — PUSHED 2026-09-11 (PAT via per-command helper,
never persisted). Remote CI run **34660481546** on **1c55941e**
= **SUCCESS** (13/13, llm-smoke skipped by design). Observation loop
closed. (Branch stacks TASK-030; pushed after TASK-030's own green.)

## Branch stacking (push order matters)
`feature/TASK-031-deepseek-flash-model-lineup` was cut from the tip
of `feature/TASK-030-pulse-productization-pass`, NOT from main: it
stacks all 5 TASK-030 commits underneath its own. Push/merge
TASK-030 first, then TASK-031 (fast-forward) — or merge TASK-031
alone, which carries the reviewed+green TASK-030 work with it.

## Residuals
- Live vision round-trip unverified (no key here) — fakes pin the
  exact request bytes instead.
- Legacy names still serve; explicitly-set ones keep working.
- ext-fileinfo undeclared in composer.json (handled degradation).
