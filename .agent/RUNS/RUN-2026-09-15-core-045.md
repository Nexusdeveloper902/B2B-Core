# RUN-2026-09-15-core-045 — TASK-044 multipart HMAC canonical (backend half)

## What was done
Backend half of the station classify-401 fix, in dependency order:
signer first (`DeviceRequestSigner::multipartCanonical()` +
`canonicalBody()`, `verify()` switched to the canonical — JSON paths
byte-identical), then 4 regression tests in `DeviceHmacAuthTest`
(canonical pin, signed classify 200, swapped-image 401 + zero
deposits, signed capture 200), then `docs/API.md` + `.es.md`
(auth-models row + classify/capture auth lines), then Pint + quality.
Firmware half (B2B-Firmware TASK-014, ADR-017) signs the mirrored
canonical via `EspApiClient::postMultipart()`; the shared
`sha256('abc')` literal is asserted on both sides. Contract (ADR-063,
amends ADR-062): classify = `event_id + image.sha256`, capture =
`image.sha256` — never the raw multipart PHP cannot see.

## Gates
- `./run test feature --filter='DeviceHmacAuthTest|ClassificationTest|CaptureFlowTest'`:
  30 passed / 130 assertions (13 DeviceHmacAuth incl. 4 new).
- `./run quality`: PASSED (Pint clean after one autofix pass on the
  signer; docs parity EN+ES green; shellcheck skipped locally as
  usual — CI enforces).
- Firmware gates (other repo, same session): native 128/128 (+3),
  `esp32cam` + `esp32dev` SUCCESS.

## Traps
- `php://input` / `$request->getContent()` is EMPTY for
  `multipart/form-data` — any body-bound signature over "raw bytes"
  silently becomes a signature over `""` server-side. (This was the
  whole bug.)
- `UploadedFile::fake()->image()` content is stable per object: hash
  the file BEFORE the test call and the server recomputes the same
  hash — no flake.
- `default.edit` errors on this box despite applying; the working
  pair is `filesystem_edit_file` / `filesystem_write_file`.

## Status
DONE 2026-09-15 — code + tests + docs + memory in place; field
stations need the `hce.18` reflash (kept-arm retry keeps failing on
`hce.17` by design — visible, not silent).
