# RUN-2026-09-04-core-006 — activity ledger

| Time (approx) | Activity | Evidence / result |
|---|---|---|
| start | Read worklog + .agent memory; repo reality: main @ 4f2dcfc, TASK-002..007 complete, CI green | worklog.md, git log |
| +10m | Audited all 13 shell files for Linux-isms (uname/pgrep/pkill/venv bin//tmp image/python3/.tools ELF assumptions) | scripts/* reads |
| +25m | common.sh: detect_os + B2B_OS override seam + is_windows; php_candidates excludes the Linux ELF on windows; composer_candidates +bat/.cmd/.phar probes; resolve_composer/composer_cmd dual mode; python_resolve (py launcher); windows install guidance in resolve_php | commit 92ce002 |
| +45m | doctor/status/model-server/provision-toolchain windows branches (venv Scripts, taskkill via /proc winpid, composer-only toolchain); e2e image project-relative (native curl can't read msys /tmp) | commit 92ce002 |
| +55m | run.cmd (CRLF, ASCII echoes, B2B_BASH override, well-known paths, PATH fallback) + .gitattributes line-ending contract MERGED with the skeleton's existing rules (first Write clobbered them — caught by git status, restored) | commit 92ce002 |
| +70m | windows-smoke CI job (windows-latest, shell bash, choco shellcheck, non-optional); dropped the composer-cache step (bare `composer` unresolvable by name in Git Bash) | commit b4dc326 |
| +85m | ScriptSuiteTest: +10 Windows contracts + cross-platform guards (is_executable skip, forward-slash paths, `bash run help`); local suite 129 passed / 1 opt-in skip; quality + e2e 22/22 + doctor all green (Linux unchanged) | ./run test/quality/e2e/doctor |
| +100m | Push + dispatch: run 33815966369 — **13/13 success FIRST TRY** incl. Windows smoke (auto-detected B2B_OS=windows, 125 tests, e2e 22/22, shellcheck via choco) | run + job logs |
| +110m | Windows log forensics found OBS-010: `php composer.bat --version` exits 0 printing `@ECHO off` (PHP executes plain cmd text) — php-mode validation false-positives for .bat/.cmd; on bat-only machines composer_cmd would pick the wrong mode. Fixed: bat/cmd validated + invoked DIRECT-only in resolve_composer + doctor | commit a951b06 |
| +125m | Re-dispatch: run 33816386157 — 13/13 success; windows log now `composer.bat → Composer version 2.10.3 (direct — Windows wrapper)` (real version, correct mode) | run + job logs |
| +135m | Fast-forward merge to main, pushed a951b06; full-diff secret scan 0 matches | git push |
| end | Push-triggered tip run 33816810463: **success** (13/13). Protocol closure: TASK-008, ADR-017, OBS-010, this run record + ledger + snapshot, PROJECT.md facts, worklog | this record |

Tool failures encountered: `.gitattributes` Write initially REPLACED
the Laravel skeleton's existing rules (git status showed M — restored by
merging, see ledger +25m). No CI repair loop was needed beyond the
OBS-010 fix (two dispatches, both green). No secret ever entered git
history.
