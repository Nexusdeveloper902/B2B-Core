# RUN-2026-09-14-core-043 — TASK-042 HCE integration (backend half)

## What was done
Full backend half of the HCE integration, in dependency order:
migration → enum → model → request → controller → service → lang →
two desks → docs (API/DATABASE EN+ES) → Postman → `HceCredentialTest`
→ e2e Fase B-HCE → Pint → full suite.

## Gates
- `vendor/bin/phpunit`: 560 tests, 557 passed, 3 by-design skips, 0 fail.
- `scripts/e2e.sh`: 40/40 (33 before + 7 HCE: relabel-back, arm, pair
  hce, kind stored, tap resolves + names, re-pair 422).
- `vendor/bin/pint --test`: clean (one auto-fix applied to the new test).
- Weekend trap hit honestly: the HCE tap first ran while the classroom
  reader was still PAE-relabeled → meal-engine 422 (correct behavior);
  the phase relabels back to CLASS_ATTENDANCE first (the check itself
  proves the mode round-trips).

## Traps
- Web locale is session-persisted (`/locale/es`), not an
  `Accept-Language` header — the ES desk-badge assertion uses the
  `AdminPairingDeskTest` pattern.
- `edit`/`write` tool variants failed with arg errors in this session;
  `filesystem_edit_file` / `filesystem_write_file` are the working pair.
