# RUN-2026-09-06-core-016 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection

- Worklog + git state first: main @ 58c4541 (TASK-017 merge),
  tree clean; STATE snapshot core-015 current
- Actions API (read-only, PAT from the git remote — never written
  to tracked files): run list for main + feature branches; jobs +
  downloaded logs for the 4 failing jobs of run 34002796038 (Lint,
  Scripts lint, Windows smoke, Arch smoke → all die on ONE
  shellcheck warning, serve.sh:77 SC2034)
- Cross-check: TASK-016 runs (34001719001 / 34001718779) failed the
  same 4 jobs — failure introduced with TASK-016's serve.sh, not
  TASK-017's UI work; TASK-015 (33998421622) was the last green run
- Logs stored to /home/z/my-project/ci-logs/ (sandbox artifacts)

## State-modifying commands

- shellcheck v0.10.0 downloaded to /home/z/my-project/tools/
  shellcheck (sandbox artifact, NOT a repo file — closes the
  local-vs-CI gap that let this slip through two merges)
- git switch -c feature/TASK-018-ci-shellcheck-red
- File edit: scripts/serve.sh — probe counter `i` now used in the
  bilingual startup-failure warning ("after N probe(s) / tras N
  intento(s)") + explanatory comment; loop behavior unchanged
- File creates: .agent/TASKS/TASK-018-ci-shellcheck-red.md,
  .agent/RUNS/RUN-2026-09-06-core-016.md (+this ledger), STATE
  snapshot; PROJECT.md facts append
- Gates (tools on PATH so shellcheck is enforced locally):
  quality PASS · test 219/3 · e2e 24/24 (serve boot exercises the
  modified loop; realtime hello + 401 green)
- Failure-path simulation: exhausted loop → "after 10 probe(s)";
  early process death → "after 1 probe(s)"
- git add + commit + switch main + merge --no-ff (explicit ref) +
  push (main + feature branch) — hashes in the worklog Stage Summary
- CI poll via Actions API until the new main run completes; all 13
  jobs green (recorded in TASK-018 acceptance)
