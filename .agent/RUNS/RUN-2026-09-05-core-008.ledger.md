# RUN-2026-09-05-core-008 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection
- `git clone` B2B-Core (main @ 5fa3b46, clean tree) + B2B-Firmware (empty)
- Read: .agent/PROJECT.md, TASKS/002-009 (numbering → next is TASK-010),
  STATE/SNAPSHOT-RUN-2026-09-05-core-007.md, RUNS history,
  routes/api.php, TapEventController, ReaderModeController,
  RedemptionController, ResolveReaderToken, EnsureRole, SetApiLocale,
  TapService, PointsService (lock convention), TapEventRequest,
  TapEventTest, ApiLocalizationTest, ReaderModeTest, DocumentationTest,
  TestCase helpers, DemoSeeder, Student/Card/Reader models, lang/en+es,
  config/presence.php, AppServiceProvider, docs/API.md + .es.md,
  docs/postman_collection.json, bootstrap/app.php, scripts/e2e.sh
  (auth patterns: PAT minting, key extraction)

## Environment provisioning (sandbox only, NOT repo changes)
- PlatformIO 6.1.19 via venv pip (firmware side)
- PHP 8.4.24 via `apt-get download php8.4-*` + `dpkg-deb -x` →
  /home/z/my-project/tools/php84 + wrapper; composer 2.10.3 phar;
  exposed to ./run via B2B_PHP (ADR-010 chain)
- libzip5 deb downloaded+extracted for the zip extension

## State-modifying commands
- cp .env.example .env; php artisan key:generate; touch database/database.sqlite
- php artisan migrate (baseline: all 13 migrations)
- php artisan migrate (feature: adds 2026_09_05_000001)
- php artisan migrate --seed --force (×2, throwaway pairing_check DBs)
- php artisan serve (throwaway DB, port 8123 — live verification, stopped)
- php artisan test (multiple runs); php vendor/bin/pint (fix mode once)
- ./run e2e (B2B_PHP override) — throwaway e2e.sqlite, server stopped
- php artisan package:discover (after first composer install quirk)

## Tests / builds
- Baseline: php artisan test → 127 passed, 3 skipped
- Feature: CardPairingTest → 14 passed (55 assertions)
- Full: 141 passed, 3 skipped, 2677 assertions; Pint PASS; e2e 22/22
- Live curl pairing verification script → 20/20

## Dependency changes
- composer install (lockfile untouched); no new PHP dependencies

## Git operations
- Branch: feature/TASK-010-card-pairing-endpoint (from main @ 5fa3b46)
- Commits: caa1d82 (data layer), 09a76d8 (endpoints+tests), 1a4beba (docs)
- Merge: --no-ff into main (merge commit recorded in the snapshot)
- Push: origin main + feature branch (PAT-authenticated remote; the token
  exists only in the remote URL / git credential, never in tracked files)

## Failures
- composer post-autoload-dump package:discover failed on first install
  (partial vendor state) — succeeds on re-run; documented as a discovery
- First pairing test run: 13 failures — test helper assumed a cardless
  seeded student (none exists); fixed by creating fresh students in tests
- Guest-401 assertion ordering (actingAs session leak within a test) —
  fixed by reordering; not an app defect
- Live-verification script had two self-inflicted check bugs (unicode-
  escaped ES messages; missing Content-Type on a curl) — fixed; app
  behavior was correct throughout

## Discoveries
- See RUN-2026-09-05-core-008.md "Discoveries" (composer first-run quirk;
  sandbox PHP provisioning pattern)
