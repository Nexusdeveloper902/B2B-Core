# RUN RUN-2026-09-02-core-001

## Task
TASK-002-core-platform-mvp

## Agent Role
Full-Stack Engineer (Laravel / Blade)

## Result
COMPLETED (with one environment-specific, honestly-reported blocker: live
end-to-end Phase E LLM calls are geo-blocked from this build environment's
network — the Gemini API refuses the region; see Discoveries. All other
acceptance criteria pass, and Phase E's live path works from unrestricted
networks, e.g. the owner's local environment.)

## Phases Addressed This Run
- Phase A: DONE
- Phase B: DONE
- Phase C: DONE
- Phase D: DONE
- Phase E: DONE (scaffold + tests); live end-to-end BLOCKED (environment, not code)
- Phase F: DONE
- Phase G: DONE
- CI/testing mandate (owner-supplied addition): DONE — exceeds the task's
  minimum test requirements with a matrix CI pipeline, secret scanning, and a
  real-HTTP e2e runner
- Bilingual mandate (owner-supplied addition): DONE — app + docs in EN/ES
- Local-model mandate (owner-supplied addition): DONE — local driver +
  documented contract + reference server (ADR-007)

## Summary
First execution against this repository (it was completely empty). Built the
entire Presence Platform core MVP in one run on Laravel 13.30/SQLite: the
event-type spine schema, the tap ingestion loop with reader Bearer auth, reader
relabeling, idempotent recycling classification behind the swappable
MaterialClassifier contract (stub/local/gemini drivers), the earn-and-spend
points ledger with transactional redemption, the Gemini flash function-calling
NL query with honest blocked states, bilingual EN/ES dashboards
(teacher/admin/parent), the Postman collection, bilingual docs (README, API,
LOCAL_MODEL), an 88-test pyramid (unit/integration/E2E) plus a 22-check
real-HTTP e2e script, and a 7-job GitHub Actions CI pipeline with secret
scanning. All 88 tests pass; Pint is clean; main was verified from a fresh
clone (install → migrate → seed → tests → real-HTTP e2e → live tap EN+ES) and
pushed.

## Changes Made
- 14 phase-tagged commits on feature/TASK-002-core-platform-mvp; main
  initialized at the branch tip (the repository previously had zero commits —
  there was no prior state to merge against; documented in the task record)
- Full app/ tree (Enums, Models, Contracts, Http, Services incl. Recycling
  and NlQuery), database migrations + DemoSeeder, lang/en+lang/es,
  config/presence + config/recycling, routes/api + routes/web, Blade views +
  CSS (no build step), tests/{Unit,Feature,E2E}, docs/ (Postman + API EN/ES +
  LOCAL_MODEL EN/ES), scripts/e2e.sh + scripts/local-model-server/,
  .github/workflows/ci.yml, README.md + README.es.md, .agent/ persistent memory

## Files Changed
(approx. 120 files added; see `git show --stat` per commit in
.agent/TASKS/TASK-002-core-platform-mvp.md)

## Commits Created
- 5c39a6d — feat(phase-a): skeleton + schema, models, config, bilingual seeder
- cdf5788 — feat(phase-b): core loop wiring — reader bearer auth, stateful API, EN/ES API locale
- a33a386 — feat(phase-c): classification + earn behind the swappable contract
- 87a8634 — feat(phase-d): redemption (spend half)
- 7c0a197 — feat(phase-e): NL query with Gemini function-calling
- 3d483a5 — feat(phase-f): EN/ES bilingual dashboards
- 77a3c00 — test(phase-g): full test pyramid (amended: provider binding + secrets-pattern test)
- 6142167 — docs(phase-g): Postman + bilingual API reference
- 5d70054 — ci: GitHub Actions + real-HTTP e2e runner + local model server
- eb7e8cf — docs: bilingual README
- 0c7f667 — docs(agent): persistent project memory (ADRs 001-008, architecture, observations)
- c2aae45 — chore: base controller, factories, database/.gitignore
- e463611 — chore: skeleton asset stubs
- (main = e463611 after initialization; also contains commit 4c5ab2c→77a3c00 amend history flattened into the branch commits listed above)

## Branches
- feature/TASK-002-core-platform-mvp (14 commits) — pushed
- main (initialized at feature tip e463611) — pushed

## Merge Status
- Merged: YES (main initialized from the feature branch tip because the
  repository had zero prior commits; nothing existed to merge against)
- Main commit: e463611

## Verification
- php artisan test: PASS (88 passed, 1 skipped [live-LLM opt-in], 657 assertions — fresh clone)
- vendor/bin/pint --test: PASS (107 files)
- php artisan migrate --seed on a fresh clone: PASS (credentials printed bilingual)
- scripts/e2e.sh (REAL HTTP: serve + curl, throwaway DB): PASS 22/22
- Live tap on main (fresh clone + serve + curl): PASS (EN + ES messages)
- Live curl checks of every endpoint incl. 401/403/404/422/503 paths: PASS
- Live Gemini NL query from this environment: BLOCKED — HTTP 400 "User
  location is not supported for the API use" (geo-restriction on the sandbox
  network; the key itself is expected to work from the owner's environment;
  mocked-transport tests cover the full function-calling protocol)
- Secret hygiene: no secret values committed (.env gitignored; the secrets
  test uses credential PATTERNS, not literal values; gitleaks job in CI)

## Discoveries
- Gemini API is geo-blocked from this build environment (OBS-002) — affects
  only live end-to-end verification, not the implementation
- Laravel feature tests cache the controller instance on the Route object
  after the first request — mid-test swaps need the mutable-fake pattern (OBS-003)
- `php artisan serve` requires --no-reload to pass host env vars (DB_DATABASE
  overrides for throwaway databases) to the built-in server (OBS-001)
- The repository had NO pre-existing state (empty repo) — this was a genuine
  first run; the protocol's "verify previous work" steps resolved trivially

## Decisions
- ADR-001 SQLite · ADR-002 reader Bearer auth · ADR-003 classifier interface
  (+ sanctioned stub) · ADR-004 assumed reward catalog (OWNER ACTION: confirm
  or replace) · ADR-005 LLM-credential blocker semantics · ADR-006 Gemini
  (flash) as the LLM provider · ADR-007 local-first classifier driver ·
  ADR-008 bilingual EN/ES implementation

## Problems / Blockers
- Phase E live end-to-end: BLOCKED from this environment (Gemini geo-block).
  Not a code defect; endpoint returns honest 503 blocked states here and is
  expected to work from unrestricted networks. Next agent/owner: run
  `RUN_LIVE_LLM_TESTS=1 GEMINI_API_KEY=<key> php artisan test --filter=NlQueryApiTest`
  from the owner's network to record the live PASS.

## Remaining Work (by phase)
- Phase A–G: none (all acceptance criteria evaluated below)
- Follow-up (separate tasks, not blockers):
  1. OWNER: confirm/replace the assumed reward catalog (ADR-004)
  2. OWNER: live-verify Phase E from a non-restricted network
  3. FUTURE TASK: train + deploy a real material classifier behind the local
     inference contract (ADR-007) once data/hardware exist
  4. FUTURE: real parent accounts (Phase F simplification is documented)
  5. FUTURE: tap-to-redeem flow at the recycling reader (currently desk-only)

## Acceptance Criteria Evaluation
Phase A: [x] migrations run cleanly [x] seeder creates all demo data
[x] seeder prints credential_uids + reader api_keys (bilingual)
Phase B: [x] tap authenticates via reader Bearer [x] valid tap creates event
+ device-feedback JSON [x] unknown/inactive card → clean 404 [x] bad token →
401 [x] admin-only mode change (teacher 403, guest 401, invalid type 422)
Phase C: [x] multipart classify [x] swappable interface + stub [x] points per
config table into ledger [x] idempotent against double submission
Phase D: [x] redeem deducts on sufficient balance [x] clean 422 + shortfall on
insufficient (transaction + row lock against races)
Phase E: [x] scaffold with real Eloquent-backed functions (unit-tested)
[x] live path implemented; [ ] live end-to-end from THIS environment —
BLOCKED (geo), documented per the protocol; works from unrestricted networks
Phase F: [x] login works [x] teacher dashboard [x] admin dashboard (stats +
mode control + NL box) [x] parent timeline view
Phase G: [x] Postman collection covers all endpoints [x] API.md EN+ES [x]
automated tests cover the core loop (tap success/failure, classify
idempotency, redemption failure) — plus unit, e2e, localization, docs, secrets
Owner additions: [x] bilingual app+docs [x] local-model classifier path
[x] CI (lint/unit-matrix/integration/e2e/http-e2e/gitleaks/optional live-LLM)
[x] no committed secrets

## Next Agent Notes
- Read .agent/PROJECT.md (has run-specific facts), the task file, and the
  ADRs before changing anything.
- The classifier swap is config-only; tests swap the container binding —
  use the mutable-fake-before-first-request pattern (OBS-003).
- Run scripts/e2e.sh for the fastest full-stack regression (throwaway DB,
  real HTTP). `php artisan test` for the pyramid.
- The e2e script forces GEMINI_API_KEY="" for deterministic blocked-state
  checks; do not "fix" that.
- Database: SQLite at database/database.sqlite (gitignored); e2e uses
  database/e2e.sqlite (throwaway).
