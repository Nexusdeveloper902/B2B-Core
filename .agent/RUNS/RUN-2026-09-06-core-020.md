# RUN-2026-09-06-core-020 — TASK-022: provider migration Gemini → DeepSeek V4 Flash

Owner directive: "Change the provider from gemini to deepseek v4
flash, pull their docs from the web." (Stateless-agent protocol; the
other two repos — Marketplace, Firmware — were inspected and contain
no LLM code, so all changes landed in B2B-Core.)

## Investigate-first

- Fresh clone of all three repos (token-safe credential helper; PAT
  never written to any file); worklog was empty (sandbox reset), so
  ALL project context came from .agent/ persistent memory: PROJECT.md
  (39 KB), TASK-006/007 history, ADR-005/006/007/015/016, OBS-002/
  008/012, latest STATE snapshot (core-019).
- Baseline truth BEFORE any edit: hermetic toolchain provisioned
  (`./run toolchain` → static PHP 8.4.23 + Composer 2.10.3), `./run
  setup`, then `./run test` = **235 passed / 1 skipped / 3564
  assertions, 13.6 s** — starting green, so every later delta is
  attributable to this run.
- Docs pulled live from api-docs.deepseek.com (web search + page
  reader): first-call, models & pricing, error codes, changelog, chat
  completions reference, vision / tool-calls / JSON-output /
  thinking-mode guides. Findings recorded as OBS-013; decision
  recorded as ADR-030 (supersedes ADR-006 + ADR-015's wire format).

## Changes

- NEW `app/Services/NlQuery/DeepSeekClient.php` (OpenAI-format
  chat/completions; Bearer auth; tools envelope; thinking disabled;
  temperature 0; typed error mapping per the DeepSeek error table;
  verbatim assistant-message round-trip) — DELETED GeminiClient.
- NEW `app/Services/Recycling/Drivers/DeepSeekClassifier.php`
  (deepseek-v4-flash-vision-exp; image_url data-URL parts;
  response_format json_object + prompt contract; max_tokens 200) —
  DELETED GeminiClassifier. Driver key `gemini` → `deepseek` in
  ClassifierFactory.
- NlQueryService rewritten for the message format: system prompt,
  verbatim assistant echo, one role:"tool" message per tool call
  (parallel calls handled), MAX_TOOL_ROUNDS unchanged (3).
- NlQueryException: +insufficientBalance (402), regionUnsupported
  retired; controller + bilingual lang messages updated
  (llm_insufficient_balance; DEEPSEEK_* names in every hint).
- Config/env/tests/CI/scripts/docs swap GEMINI_* → DEEPSEEK_*:
  config/recycling.php, .env.example, phpunit.xml, AppServiceProvider,
  CI llm-gate + live-llm-smoke (probe → chat/completions; model list
  via GET /models), scripts/llm-check.sh (full rewrite — DeepSeek
  verdicts incl. 402 top-up guidance, legacy-name + vision-model
  warnings), scripts/status.sh, test.sh, e2e.sh, run dispatcher help.
- Tests: NEW DeepSeekClientTest (9 cases, docs-derived fixtures);
  NlQueryServiceTest/NlQueryApiTest/FullJourneyTest/DashboardTest
  migrated (balance blocker replaces the region blocker); the
  GeminiClientTest deleted; DocumentationTest tripwire + DeepSeek
  sk- pattern (old AIza/AQ patterns kept — history defense).
- Bilingual docs updated: README EN/ES, docs/API EN/ES (blocked-
  reason table now DeepSeek's, incl. llm_insufficient_balance),
  LOCAL_MODEL EN/ES, SCRIPTS EN/ES, postman_collection.json.

## Gates

- `./run test` **238 passed / 1 skipped / 3914 assertions** (baseline
  235/1/3564; +3: DeepSeekClientTest has 9 vs GeminiClientTest's 6,
  +1 balance blocker feature test)
- `./run quality` PASS — after Pint auto-fixed 5 style issues in the
  new files (ordered imports / single quotes / brace positions);
  shellcheck 0.10.0 (CI-parity version, sandbox artifact at
  /home/z/my-project/tools/shellcheck) clean; docs parity holds
- `./run e2e` 24/24 (real HTTP)
- `./run llm-check` exit 2 with bilingual not-configured guidance
  (no DeepSeek key exists in this environment — see Problems)

## Problems / Blockers

- LIVE round-trip NOT verified: no DEEPSEEK_API_KEY value was
  supplied this run (only a GitHub PAT). The full protocol is
  unit-locked against the documented contract; the CI live-llm-smoke
  job (now reading the DEEPSEEK_API_KEY secret) re-proves the moment
  the owner stores a key. Follow-up recorded in TASK-022.
- CI on GitHub: the workflow was migrated but the live-smoke job
  will gate OFF (secret absent) — expected, honest skip, never a
  masked failure. NOTE: pushes from this sandbox may emit no GitHub
  events (OBS-006) — dispatch via REST workflow_dispatch if the push
  run does not appear.

## Push record

- Branch `feature/TASK-022-deepseek-provider-migration`, commit,
  merge --no-ff into main, push both (token passed via a one-time
  credential-helper env var — never in a remote URL, config file,
  commit, or record). CI dispatch/poll noted in the ledger.
