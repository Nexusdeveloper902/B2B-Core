# RUN-2026-09-06-core-018 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection

- Worklog + git state first: main @ b9869e8 (TASK-019 Signal
  redesign merged), tree clean, CI 13/13 green; STATE snapshot
  core-017 current
- Pairing flow read end-to-end: PairingService, ArmPairing /
  CardPairing / PairingStatus / AdminPairing controllers,
  PendingPairing model, pairing.blade.php desk script,
  RealtimeServeCommand + realtime.js + RealtimeToken/Feed, serve.sh,
  firmware main.cpp + e2e_backend.sh (throwaway DB — not the dev
  DB), DemoSeeder (seeds NO pending pairings), config presence +
  realtime, lang EN/ES, the 5 pinning test files
- Dev DB inspected: pending_pairings table EMPTY (post-TASK-019
  reset) — the owner's 12468 s row lived in their environment;
  code-path audit found no writer beyond now()+45 s

## State-modifying commands (sandbox, throwaway DB unless noted)

- Repro driver: /home/z/my-project/scripts/repro-pairing.sh
  (sandbox artifact, NOT a repo file) — copy of dev DB →
  /tmp/repro.sqlite; php artisan serve on 8019; login; arm ×2;
  reader-key pair; status polling; far-future expires_at plant;
  ran pre-fix (zombie + 30599 s confirmed) and post-fix (both dead)
- Live browser proof: throwaway /tmp/t20-proof.sqlite; web :8020 +
  realtime:serve :8082; agent-browser login → /admin/pairing; arm
  button click; curl pair ×2; WS kill + cross-process arm; WS
  restart; 7 screenshots t20-0*.png under /home/z/my-project/scripts/
- One stray helper arm hit the DEV DB (mis-env'd php -r before the
  DB_DATABASE flag was set): removed immediately; dev DB verified
  back to 0 pending rows, 0 stray cards
- Test runs: ./run quality (Pint fix applied to new e2e code),
  ./run test (232/3), ./run e2e (24/24)
- git switch -c feature/TASK-020-pairing-liveness
- File creates: app/Services/Realtime/RealtimePairing.php,
  tests/Unit/Realtime/RealtimePairingTest.php,
  .agent/DECISIONS/ADR-029-pairing-channel-single-window.md,
  .agent/TASKS/TASK-020-pairing-liveness.md, .agent/RUNS/
  RUN-2026-09-06-core-018.md (+this ledger), STATE snapshot
- File edits: PairingService (arm supersede + statusPayload + pair
  retire), PendingPairing (active guard), PairingStatusController
  (shared payload), RealtimeServeCommand (pairing channel +
  admin-only delivery), realtime.js (generalized boot +
  realtime:pairing), pairing.blade.php (badge, boot node,
  applyStatus, clamp, local expiry), AdminPairingController (token
  mint), RealtimeServerTest (+2 tests, user seeding), PairingStatus
  Test (+2), CardPairingTest (1 strengthened), AdminPairingDeskTest
  (+1), FullJourneyTest (pairing leg), realtime-feed.md contract,
  README.md + README.es.md (desk row)
- Commit + merge --no-ff + push (see RUN file); Actions polled to
  13/13 green
