# RUN-2026-09-06-core-017 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection

- Worklog + git state first: main @ d2f98ff (TASK-018 CI fix
  merged), tree clean; STATE snapshot core-016 current
- Cloned B2B-Marketplace via PAT-from-remote (read-only study):
  main @ 5ae6343, design system "Signal" (its TASK-012 v2) —
  tokens block, app.css (1513 lines), app.js motion architecture,
  layout/header/footer, fonts.css; anime.esm.min.js md5 fbfdf1a7
- Core UI inventory: 9 views + 5 components + 6 view partials
  (1151 lines blade), tokens.css (70) + app.css (936) +
  realtime.js (221), all JS-facing selectors and the TASK-017 test
  pins mapped before writing a line

## State-modifying commands

- git switch -c feature/TASK-019-signal-redesign
- File creates: public/js/motion.js,
  public/js/vendor/anime.esm.min.js (copied byte-identical from the
  marketplace clone; md5 verified),
  .agent/DECISIONS/ADR-028-signal-redesign.md,
  .agent/TASKS/TASK-019-signal-redesign.md, .agent/RUNS/
  RUN-2026-09-06-core-017.md (+this ledger), STATE snapshot
- File rewrites: public/css/tokens.css (Signal token block 1:1 +
  Core-only --shell/--tap), public/css/app.css (full Signal
  component rewrite; every JS-facing selector and pinned rule kept:
  .stat-strip .stat:first-child → font-size 2.5rem,
  .live-chip[data-event-type^=...] tone mapping,
  .ledger-table[data-stack] tbody td::before)
- File edits: layouts/app.blade.php (inline .js-motion gate,
  motion.js module, <details> mobilenav with logout, welcome
  wrapped in .topbar-user-name), light data-reveal attributes on
  admin/dashboard, admin/pairing, teacher/dashboard,
  parent/timeline, auth/login; README.md design-system section
  ("Signal"); tests/Feature/Web/DashboardTest.php (+4 pins)
- Bench servers: raw php -S 127.0.0.1:8000 -t public +
  artisan realtime:serve :8081 (RUN-014 sandbox pattern); reader
  key + card UIDs re-read via tinker (reseed rotation — known);
  live taps event ids 1-3 (Maria, Carlos, Diego — all Late,
  Bogota 20:35-20:36) + one pairing arm→low window (Ana, expired)
- Browser proofs: 13 screenshots → /home/z/my-project/scripts/
  t19-*.png (sandbox artifacts); one mobile overflow bug found by
  the element-overflow probe (mode-form at 390 px) → fixed in
  app.css (column-stack + topbar tighten) → re-verified zero
  h-scroll on 5 pages; zero console errors throughout
- Gates: ./run test 219→223 (+4) → Pint fix on new test code →
  223/3 re-run; quality PASS (shellcheck enforced); e2e 24/24
- PAT scan over changed files: clean (gitleaks enforced in CI too)
- git add + commit + switch main + merge --no-ff (explicit ref) +
  push (main + feature branch) — hashes in the worklog Stage
  Summary
- Post-merge: CI watched via the Actions API until green
