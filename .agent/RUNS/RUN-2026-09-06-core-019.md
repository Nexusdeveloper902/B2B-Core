# RUN-2026-09-06-core-019 — TASK-021: pairing status card → Signal strip

Owner directive: "small fix but you still need the whole protocol
for this, im gonna need you to please fix the Pairing status …
little card bc for some reason it looks inconsistent with the rest
of the app"

## Investigate-first

- Worklog + git state: main @ 2cfb517 (TASK-020 merged), tree
  clean, CI 13/13; STATE snapshot core-018 current.
- Read the desk view (pairing.blade.php), app.css live-panel /
  nl-answer / countdown / live-empty rules, the marketplace
  reference's `.tap-visual` / `.form-alert` grammar, and the five
  pinning test files (AdminPairingDeskTest, PairingStatusTest,
  RealtimePairingTest, DashboardTest CSS pins, E2E journeys).
- Dev sandbox env NOTE: the tools/php84 raw binary lacks ext-iconv
  (Dotenv → mb_convert_encoding polyfill → "Class config does not
  exist" cascade that masked the real fatal). Servers MUST run via
  the `/home/z/my-project/tools/php` wrapper (PHPRC + LD_LIBRARY_PATH).
  Stale `php -S` (PID file-less, from a prior session) killed and
  restarted through the wrapper: web :8000 + realtime:serve :8081.

## Measurements that drove the fix (agent-browser, 1280px)

- BEFORE: live-head text inset 18px; state box inset 16px+border
  (17px) with margin-top 14px, radius 6, w=518; countdown bar
  margin 14/0/8 (ZERO side inset — touched panel borders); idle
  note default <p> margins, text at panel edge.
- AFTER: head/state/bar identical w=518 @x=651; state pad 13px
  18px, margin 0, radius 0, tint field + 1px rule; bar margin 0
  radius 0 full-bleed meter; idle note live-empty (24px 18px mono).

## Changes

- public/css/app.css: `.live-panel .nl-answer` (+ answer-ok/error)
  + `.live-panel .countdown` scoped strip grammar; standalone
  `.live-empty` rule (shared with the feed's empty row).
- resources/views/admin/pairing.blade.php: idle note
  `class="muted"` → `class="live-empty"`; comment updated.
- tests/Feature/Web/AdminPairingDeskTest.php: +1 pin
  (the_status_box_follows_the_live_panel_full_bleed_grammar).

## Gates

- `./run quality` PASS (Pint 142 files, shellcheck, docs parity)
- `./run test` 233 passed / 3 skipped
- `./run e2e` 24/24
- Live proof: 7 screenshots (before×2, after armed/low/expired/
  idle×2, mobile) + VLM review; ES render; zero console errors.
