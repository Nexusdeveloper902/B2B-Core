# RUN-2026-09-06-core-014 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection

- Worklog tail + B2B-Core git state FIRST: main @ 9b859c2 (TASK-015
  merged/pushed, fresh clone green) — but the crashed session left
  TASK-016 **uncommitted on branch
  feature/TASK-016-realtime-dashboard**: ~1,700 lines across 16
  modified + 13 new paths (services, command, token route, feed
  partial, realtime.js, serve/status/e2e scripts, 33 tests, docs,
  .agent TASK/ADR/ARCHITECTURE files present but NO run records)
- Verified implementation completeness by running the gates, not by
  re-reading: suite 211/3, e2e 24/24, then browser proof
- Dev DB fixtures: 2 readers (classroom + recycling), 4 paired cards,
  2 events; reader api key used for the live tap proof (value never
  recorded)

## State-modifying commands

- B2B_PHP=/home/z/my-project/tools/php ./run test (twice — second
  run after the Pint fix), ./run quality, ./run e2e
- vendor/bin/pint tests/Feature/Web/DashboardTest.php (one
  import-order/strict-types fix; suite re-run green)
- Web boot for browser proof: `php -S 127.0.0.1:8000` (wrapper) with
  Laravel's vendor router from `public/` CWD — after TWO sandbox
  boot attempts: `artisan serve` inner child + direct router run
  both fatal (filtered env drops PHPRC/LD_LIBRARY_PATH → extensions
  gone; root cause pinned via /proc/<pid>/environ)
- Discovered a leftover `realtime:serve` (pid from the crashed
  session) still bound to :8081 — 15+ min uptime, page connected
  instantly; killed it to prove the honest-offline path, then
  restarted it for the live proofs
- Live taps through the real device endpoint (curl, classroom
  reader + two paired cards) → event ids 3 and 4 in the dev DB
- agent-browser: login (teacher session recovered from TASK-014
  state, then admin re-login), DOM evals, screenshots to
  /home/z/my-project/scripts/task016-*.png (sandbox artifacts, not
  repo files)
- `./run serve` boot + SIGINT teardown test (both ports open → both
  closed, trap cleanup proven)
- File creates: .agent/RUNS/RUN-2026-09-06-core-014.md (+this
  ledger), STATE snapshot, PROJECT.md facts append
- git add + commit + switch main + merge --no-ff (explicit ref) +
  push (main + feature branch) — hashes in the worklog Stage Summary
- Post-merge: dev DB re-seeded (`./run reset --force`) — the live
  bench taps (event ids 3/4) replaced by the standard demo state
