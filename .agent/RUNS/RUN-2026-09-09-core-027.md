# RUN-2026-09-09-core-027 — TASK-028: the orphaned desks get nav links

## Context
Owner: "I dont see the new GUI's" (chat, 2026-09-09). RUN-026 left
main CI-green at a4d5503 with TASK-027 complete — suspicion first
went to push/deploy state, but `main...origin/main` was clean and
in sync; the code was on GitHub. The real defect was in the tree.

## Investigation
- Confirmed TASK-027 artifacts all present (routes, controllers,
  views, tests, docs) and pushed — deployment was NOT the problem.
- Read `layouts/app.blade.php`: admin nav (desktop + no-JS mobile
  menu) links only admin.dashboard / admin.pairing / admin.ecostation.
  No dashboard quick-links exist either → `/admin/students` and
  `/admin/readers` were URL-only orphans. TASK-027's tests pinned
  each desk's own surface; nothing pinned the layout reaching them.
- Verified bilingual keys `students_page`/`readers_page` already
  existed in en+es (TASK-027 added them for the desks' own titles).

## Changes
- `resources/views/layouts/app.blade.php`: Students + Readers links
  in both admin navs (workflow order: people → hardware → cards →
  eco), inside the existing `isAdmin()` gate; reuses existing keys —
  no lang-file edits, parity test untouched.
- `tests/Feature/Web/AdminDesksTest.php`: new
  `the_layout_nav_links_both_desks_for_admins_only` — hrefs + labels
  asserted for admin; assertDontSee for teacher and student views.
- `docs/FRONTEND.md` + `.es.md`: honest note — desks shipped
  URL-only, nav links them since TASK-028, pinned by a regression
  test.
- `.agent/TASKS/TASK-028-nav-discoverability.md` (this ledger entry).

## Verification (local)
- `./run test` → **314 passed / 1 skipped** (313 + the new nav
  regression), 4,771 assertions local.
- `./run quality` → PASS (pint, shellcheck, scripts/docs parity).
- `./run e2e` → **33/33**.
- No new translation keys; BilingualJourneyTest parity green.

## Status
DONE locally — push + remote CI observation below.

## Push outcome
- Commit strategy per repo convention (fix + records together,
  main direct): see git log. PAT via per-command credential helper
  only (store-file outside the repos); never in any tree, URL,
  command line, or record.
- Remote CI verdict: appended below after observing the runs this
  push triggers (trap rule: observe the run your own record commit
  triggers too).
