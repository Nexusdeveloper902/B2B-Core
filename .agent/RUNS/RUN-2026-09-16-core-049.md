# RUN-2026-09-16-core-049 — commit TASK-048 and TASK-047

## What was done
The working tree held two independent change sets with no file overlap:

- the TASK-048 reporting work, uncommitted from an earlier session;
- the TASK-047 speaker-bridge work (RUN-048).

They were committed separately, TASK-048 first because it is older, and
pushed to `origin/main`. `.agent` records were written for both:
TASK-048, ADR-066, ADR-067, the realtime-feed architecture update, and
PROJECT.md.

## Verification (combined tree, before committing)
- `./run test`: 659 tests, 656 pass, 3 by-design skips.
- `./run quality`: green. Pint clean and docs parity OK; shellcheck is
  not installed locally, and CI enforces it.
- The dev MariaDB was migrated with `tap_feedback` (additive).

## Not verified
The intermediate commit (TASK-048 alone) was not tested on its own.
The tree it represents is the one that existed before TASK-047 started.
