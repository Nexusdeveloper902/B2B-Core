# RUN-2026-09-06-core-017 — Signal UI realignment (TASK-019)

Owner directive (verbatim): "Hey, make a complete rehaul of the
thing, keep the functionality (eg rn it still says this Live feed
unavailable — reload the page to see the latest taps.) and basically
re do the whole ui so its more intuitive but i want it to be 0 ai
slop, and make it consistent with the whole styling on
https://github.com/Nexusdeveloper902/B2B-Marketplace"

## Reference study (the source of truth)

Cloned B2B-Marketplace (main @ 5ae6343) and read its live design
system "Signal" (its TASK-012 redesign v2, ADR-014 there): dark
shadow-grey 950 ground / 900 raised / 800 surfaces; scarlet primary
(solid 600 CTAs), muted-teal data color, tiger-orange sparing
accent; Space Grotesk display / IBM Plex Sans body / IBM Plex Mono
data; 10/6 px radii; ruled editorial sections (2 px text rules,
1 px soft dividers, 7 px square markers); contact-form field pattern
(teal focus rings, scarlet error tints); wordmark with the 12 px tap
square; sticky blurred topbar with underline-active nav; native
`<details>` mobile menu; anime.js v4.5.0 vendored ESM + onScroll
reveals gated behind `.js-motion` (never under reduced-motion);
`:focus-visible` 2 px accent-soft floor; scarlet selection.

## What was done (ADR-028)

- `tokens.css`: the marketplace's `:root` block copied value-for-
  value (4 palette scales + semantic roles) + Core-only `--shell` /
  `--tap` appended under a clearly marked section. ADR-013's
  value-match contract restored (ADR-027's supersession reversed).
- `app.css`: full Signal rewrite (~880 lines). KPI strip → editorial
  ruled columns (2 px text rule, 1 px dividers, mono teal labels,
  Space Grotesk values, hero 2.5rem first stat — the TASK-017 pin
  holds). Panels → raised surfaces + 2 px scarlet top rule
  (panel-rule) + ledger-cap-style labels (mono uppercase + 7 px
  square). Tables → mono teal uppercase heads + row rules + mono
  num/code cells. Live feed → the ledger terminal: surface cap row,
  dot pulse per badge state (connecting orange / live teal / offline
  scarlet), grid rows (square avatar / student / chip+time),
  row-in + data-tint flash, offline hint as a scarlet-tint box.
  Stamps / sum-chips / event chips → Signal role mapping
  (present=teal, late=tiger-orange, absent=scarlet, CLASS_=teal,
  PAE_=orange, RECYCLING_=scarlet, CARD_=neutral) — CSS-only tone
  mapping via attribute selectors (pin holds). Buttons → scarlet
  primary / quiet outline; loading ring spinner kept. Forms →
  contact-form pattern on bg with teal focus rings. Login →
  contact-form card + hero-chip-style demo chips + mono aside.
  Footer → marketplace pattern (raised, note, legal strip). Mobile
  → data-stack labeled cards (pin holds), 2-col KPI, column-stacked
  mode forms, logout rides the mobile menu. Reveal states +
  reduced-motion hard-off (marketplace block).
- `motion.js` (new, 42 lines) + `vendor/anime.esm.min.js`
  (byte-identical md5 fbfdf1a7 to the marketplace's file): the
  marketplace's reveal architecture trimmed for dashboards —
  `[data-reveal]` + `[data-reveal-stagger]` via onScroll, module
  exits without `.js-motion`. No loops, no hero demos: dashboards
  stay calm.
- Layout: inline `.js-motion` gate (never under reduced-motion) +
  module script + the marketplace's `<details>` mobilenav (logout
  duplicated there for phones). Views: `data-reveal` /
  `data-reveal-stagger` attributes only — zero structural change.
- README: design-system section rewritten ("Signal", ADR-028).
- Tests: +4 pins in DashboardTest (tokens 1:1 with exact hex
  spot-checks; dark ground + scarlet selection + focus floor +
  JS-gated reveal states; layout motion gate + vendored anime +
  import line; stamp/chip tones inside the Signal roles). ONE Pint
  style fix on the new test code (quotes/attribute order).

## Verification

- `./run test` → **223 passed / 3 skipped** (was 219/3; +4)
- `./run quality` → PASS (shellcheck 0.10.0 enforced locally,
  TASK-018's fix holding) · `./run e2e` → **24/24**
- Real-HTTP live proof (raw `php -S` + `realtime:serve`, RUN-014
  sandbox pattern; reader key/card UIDs re-read from tinker after
  the reseed — known bench catch):
  - login: demo chip fills + signs in as admin; dark card, band,
    reveal (pw "SHOW" button present)
  - admin: KPI ruled strip + 5 stroke icons, badge **live**;
    real device tap (Maria) → row arrives WITHOUT reload: avatar
    "MG", teal CLASS chip, "just now", `live-new` flash; second tap
    (Carlos) → teacher dashboard rows show Late via SSR
  - teacher: live flip proven — Diego's row flipped Absent→Late
    with `js-row-flash` while the page was open
  - pairing: Arm → bar 430 px draining at 42 s → `is-low` (scarlet)
    at 7% with the armed state box intact; screenshots at both
  - mobile 390 px: KPI 2-up, thead gone, rows as cards with mono
    data-labels, nav 64 px, **zero h-scroll** on /admin /dashboard
    /admin/pairing /parent/students/1 /login (one mode-form overflow
    caught by the element-overflow probe and fixed: mode-form
    column-stacks at ≤620, topbar user+logout hide — logout rides
    the mobile menu)
  - reduced-motion (correct emulation keyword): `.js-motion` NOT
    added, `[data-reveal]` opacity 1 (visible), dot animation none
  - Spanish: "Panel del Administrador", badge "En vivo", full feed
  - honesty (owner's quoted string): WS server killed → badge
    "Desconectado" + hint visible "Canal en vivo no disponible —
    recarga la página para ver los últimos toques." (the ES twin of
    the exact EN string); WS restarted → auto-reconnect to "live",
    hint hidden again
  - **zero console errors / page errors on every page**
- Device + realtime protocol diff: routes/api.php, controllers,
  services, models, migrations, lang keys — untouched (only
  public/css, public/js (+vendor), 6 blade files, README, tests).

## Result

DELIVERED — Core's UI is now the marketplace's Signal system 1:1:
same tokens, same grammar, same motion. Test count on main: 223.
