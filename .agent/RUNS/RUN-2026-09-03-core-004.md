# RUN-2026-09-03-core-004 — Gemini key rotation + 3.1-flash-lite + live NL query (TASK-006)

Run ID: RUN-2026-09-03-core-004
Date: 2026-09-03
Task: TASK-006-gemini-key-and-model-swap
Branch: main only
Protocol: stateless agent master execution protocol (repo reality
authoritative, append-only memory, main must remain buildable).

## Owner request

> "Ok i tried actually using the GeminiAPI key, and well, nothing, it said
> its unaveliable, have this key for testing AQ.Ab8RN6K6-… use
> gemini-3.1-flash-lite"

## What was delivered

**A working live NL-query feature.** The new-format Gemini key is rotated
into the repo secret, the platform defaults to `gemini-3.1-flash-lite`,
and the live function-calling round-trip **actually executes and passes
in CI** — proven, not asserted:

- Run 33786816821 (commit 62d1db1): **12/12 jobs success**, live test
  `✓ live end to end query with a real gemini key — 3.33s`,
  `Tests: 5 passed (10 assertions)`, probe `HTTP 200` on
  `models/gemini-3.1-flash-lite:generateContent`.

Three stacked defects were found and fixed along the way (masked-skip
key delivery, dead old key, Gemini 3.x wire format) — see TASK-006,
ADR-015, OBS-007.

## Commit chain on main

- `33217ec` feat(llm): default to gemini-3.1-flash-lite + tripwire for
  AQ-format keys (13 files: config, providers, classifier, docs EN/ES,
  fixtures, .env.example, CI job name)
- `aabaa3d` fix(ci): live-LLM smoke key never reached the test — pass it
  as process env (+ skip-detection, − continue-on-error)
- `ee623b7` test(ci): raw-diagnostics probe for the live-LLM smoke job
- `62d1db1` fix(nl-query): Gemini 3.x wire format — lowercase schema
  types + verbatim model turns (+ Log::warning, + laravel.log tail)

## Test reality

- Local suite: **109 passed, 1 skipped** (the live test — correctly, no
  key in the geo-blocked sandbox), 2147 assertions. +1 new test
  (`wire_format_is_lowercase_openapi_types`).
- CI: 12/12 including the genuinely-live smoke.
- Secret hygiene: full-history scan for `AIza…`, `AQ.…`, `ghp_…` patterns
  → 0 matches; the new key exists only as the Actions repo secret
  (rotated via sealed-box REST, value never printed).

## Key evidence trail (for future runs)

- Differential auth test from the sandbox: fake `AQ.` key → 401
  UNAUTHENTICATED; real key → 400 FAILED_PRECONDITION "User location is
  not supported" → the key is VALID, OBS-002 (geo) remains the only local
  blocker; runners have no such restriction.
- Probe output shows `gemini-3.1-flash-lite` in the reachable flash family
  (alongside `gemini-flash-lite-latest`, `gemini-2.5-flash-lite`, …).
- The failing intermediate runs (33785622131, 33786354570) are INTENTIONAL
  history: they demonstrate the honesty of the repaired smoke job (dead
  key → red; skip → red; only a real live pass → green).
