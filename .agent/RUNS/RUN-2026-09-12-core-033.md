# RUN-2026-09-12-core-033 — TASK-033: A/B class per grade, grade 0 removed

Owner (chat, 2026-09-12): only "5B" exists and never changes — add
an A and a B variant for each grade; grade 0 must not exist.

## Changes
- `database/seeders/DemoSeeder.php`: 22 classes (`1° A`…`11° B`);
  5° B keeps teacher + students.
- `resources/views/admin/students.blade.php`: grade select 1–11.
- Tests: 5 files (explicit 5° B lookups; C-variant fresh names;
  grade/class-dropdown pins incl. `assertDontSee` 0°).
- `docs/FRONTEND.md` + `.es.md`: grade-range line.

## Gates
- First post-seeder run: 9 red — all single-class-world assumptions
  (`SchoolClass::first()`, fresh `6° A`/`8° A` names). No product
  bug; fixtures updated, zero production-code change beyond seeder
  + blade range.
- Final: `./run test` **443 passed / 3 skipped** · `./run quality`
  PASS (Pint + docs parity).

## Status
DONE 2026-09-12 — uncommitted tree; commit/push per owner call.
