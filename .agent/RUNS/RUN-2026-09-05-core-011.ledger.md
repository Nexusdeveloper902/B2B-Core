# RUN-2026-09-05-core-011 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection
- Worklog TASK-007/011/012 entries + B2B-Core main @ be6740c (clean)
- Schema: cards (student_id FK), events (card_id → cards cascade),
  pending_pairings (card_id → cards nullOnDelete; reader/student FKs)
- app/Services/PairingService.php (pair(): ANY existing cards row →
  422 already_paired — the invariant this task resets around),
  app/Models/{Card,PresenceEvent,PendingPairing}, config/database.php
  (sqlite foreign_key_constraints default), routes/console.php +
  bootstrap/app.php (command auto-discovery)
- Script-suite contracts: run dispatcher (script_for + usage),
  scripts/reset.sh (guard pattern to mirror), scripts/quality.sh
  (bash -n scans scripts/*.sh at maxdepth 1; docs parity derives from
  the dispatcher), scripts/_lib/common.sh (confirm/die/help_header/
  ensure_env_and_key), tests/Unit/ScriptSuiteTest.php (COMMAND_SCRIPTS
  map + all invariants it generates), tests/TestCase.php +
  tests/Feature/Api/CardPairingTest.php (arm/pair HTTP flow + 422
  message pin), tests/Unit/DocumentationTest.php (needle style),
  docs/SCRIPTS.md/.es.md (reset section as template),
  .agent/ARCHITECTURE/card-pairing-flow.md (invariant 2 + FK table),
  .agent/{TASKS→next=013, DECISIONS→next=023, STATE→core-010}

## State-modifying commands
- git checkout -b feature/TASK-013-unpair-cards-script
- File creates: app/Console/Commands/UnpairCardsCommand.php,
  scripts/unpair.sh (+x), tests/Feature/Console/
  UnpairCardsCommandTest.php, .agent/DECISIONS/ADR-023-unpair-deletes-
  card-rows.md, .agent/TASKS/TASK-013-unpair-cards-script.md,
  .agent/RUNS/RUN-2026-09-05-core-011.md(+this ledger), STATE snapshot
- File edits: run (script_for + usage), tests/Unit/ScriptSuiteTest.php
  (COMMAND_SCRIPTS + unpair), docs/SCRIPTS.md, docs/SCRIPTS.es.md,
  .agent/ARCHITECTURE/card-pairing-flow.md (append re-testing section),
  .agent/PROJECT.md (facts append)
- B2B_PHP=… ./run test feature --filter=UnpairCardsCommandTest (x3 —
  two test-side fixes), ./run test (full), ./run quality, ./run e2e
- Dev-DB live smoke: ./run reset --force → ./run unpair --force (4
  cards deleted) → ./run unpair --force (noop) → piped-decline guard
  abort → ./run reset --force (restore standard state)
- git add + commit + checkout main + merge --no-ff (explicit ref) +
  push (main + feature branch) — see RUN file Verification section
