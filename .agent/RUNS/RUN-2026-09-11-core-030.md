# RUN-2026-09-11-core-030 — TASK-030: Pulse productization pass (A/B/C + Fix 3)

## Context
Owner's consolidated Pulse rundown (chat, 2026-09-11): implement the
remaining fixes (`.agent` state + commits checked first), one item
fully tested before the next starts, testing heaviest, subagent
audits + implementation comparisons, branches only (main untouched).
Baseline on branch `feature/TASK-030-pulse-productization-pass`:
332 passed / 3 skipped, green.

## Pre-flight audit (three parallel subagents, verified vs code)
- NL pipeline works mocked (live needs DEEPSEEK_API_KEY); markdown.js
  renders; analytical functions existed with ZERO execute() tests.
- America/Bogota wall-clock consistent; no yesterday-as-today bug.
- Seeders toy-size (1 class, 4 students, 0 events, 0 deposits).
- Student auto-provision MISSING by design; reader creation + key
  lifecycle MISSING; Instagram MISSING; PTS hardcoded (5 spots);
  realtime.js fallbacks unreachable (audited, non-issue); PAE gate +
  ENTRY/EXIT + intended-URL + teacher wall existed code-only.
- Windows-side `git status` dirt (mode 755→644) is a
  `\\wsl.localhost` view artifact — Linux-side tree was clean. All
  shell work went through `wsl -d archlinux` (PowerShell 5.1 has no
  tail/grep/quoting for this work).

## Fix A — automatic student logins (ADR-044) — COMMIT 042ebd1
- `StudentAccountService` (convention email + suffix disambiguation +
  idempotent, race-retried, bulk path), provisioning inside the
  create/import transactions, display-once credentials,
  `POST …/students/{student}/account` backfill, `users.
  must_change_password` + `EnsurePasswordChanged` (web+API, JSON→403,
  DB-truth flag read) + password-change form, desk account column +
  provision buttons, DemoSeeder opts out, `unique(name,class_id)`.
- Adversarial audit verdict was REWORK — all honored: BLOCKER 4.1
  (rotation reuse refused via `different` + initial blacklist), 2.1
  (PAT wall via sanctum on-demand + DB-truth read — root-caused a
  guard-memoization false-pass with a scratch probe), 3.2 (unique
  invariant + honest 422), 3.3 (truncation counted, failed =
  dataRows-created), 1.2 (provisionMany, 505-row import 1.3 s),
  4.3 (session regenerate), 1.3/1.4/2.5/7.1/7.2.
- 37 tests: StudentAccountServiceTest (9), StudentAccountProvisioningTest
  (15), PasswordChangeTest (13).

## Fix B — reader creation + key-once lifecycle (ADR-045) — COMMIT 3cd0424
- `POST /api/v1/admin/readers` (server-minted 32-char key, display-once,
  `reader_created` frame) + `POST …/rotate-key` (old 401s, id-only audit
  log, no frame) + desk create panel/rotate confirms/live prepend +
  delegated handlers + docs/Postman/needles.
- Adversarial audit SHIP-WITH-FIXES — all honored: `{{ }}`-in-JS →
  `Js::from` (also fixed the same pre-existing class in ecostation),
  template null-guard, id/selector guards, catch UX, framesBefore
  ordering (was vacuous), api_key-injection + recycling-classify +
  boundary tests, dashboard `reader_created` limit documented.
- 13 tests: ReaderProvisioningTest (fresh key taps, rotation kills,
  log blob has no key material, desk never renders a key).

## Fix 3 — PilotSeeder + presence + i18n gaps — COMMIT da7dcb8
- `PilotSeeder` (fresh-DB-only guard): 3 classes, 24 students +
  logins + cards, 5 readers, 10 deterministic weekdays, 993 taps, 69
  deposits, 2 redemptions (~6 s). `./run reset --pilot` wired +
  bilingual script docs. DemoSeeder untouched (test fixture).
- Footer Instagram `@puls.e1681` (functional, noopener, EN/ES — the
  only channel supplied, nothing invented). `points_unit` key adopted
  (it pre-existed — first attempt duplicated it, caught by audit).
  realtime.js fallbacks proven unreachable (badge/list gating).
- Adversarial audit SHIP-WITH-FIXES — all honored (dedupe, Sotos
  comment, js-wide PTS grep, PILOT boolean, "same volumes" wording).
- 13 tests: PilotSeederTest (9, exact 993/69/2 pins hold across DBs),
  ProductizationTest (4).

## Fix C — oblivion pins for shipped-but-unproven paths — COMMIT ea6c9ad
- AnalyticalFunctionsTest (11: all six analytical execute() paths +
  scope fences + bounds + unknown-function shape),
  PaeEnrollmentGateTest (5: 422 + zero events + ES + count honesty),
  StudentSessionsTest (6: pairing/open/multi-day-leak/stray/quiet),
  IntendedRedirectTest (6: both directions, all roles, in-scope
  passthrough). Production code untouched.

## Verification (per fix, not just at end)
- Fix A: 361/3 + quality + e2e 33/33. Fix B: 382/3 + quality + e2e.
  Fix 3: 395/3 + quality + e2e. Fix C: **423/3 (8,149 assertions)** +
  quality + e2e 33/33. Three adversarial audits, zero unresolved.
- Shell traps recorded: PowerShell pipe/quoting limits (wsl + single
  quotes only); `git commit -m` via -F file; per-command `-c`
  identity (no persistent config change); lang-key appends must anchor
  on unique single lines (two near-clobbers caught same-session);
  Blade parses `{{ }}` even in JS comments; Pint runs through
  `./run quality` (manual paths scan gitignored cache).

## Status
DONE on the branch — push + remote CI observation left to the owner
(PAT handling: per-command helper, never persisted; no secret touched
any file — DocumentationTest + gitleaks patterns unchanged).

## Conscious residuals (not hidden)
- API PATs of flagged accounts are walled; device keys unaffected.
- Reader keys at rest are plaintext (rotation is the control).
- Shared initial password is documented (rotation enforced, reuse
  refused) — classroom usability won over per-student secrets (ADR-044).
- PilotSeeder includes today with full-day times (demo-full today >
  intra-day ordering purity).
- TASK-030 items G (bottle-count CV rewrite, NFC posture, R1
  self-redeem, CV training plan, trend dashboards) + marketplace
  presence stay follow-ups.

## Push outcome
- Pushed 2026-09-11 (PAT via per-command helper, never persisted).
- Remote CI verdict, FINAL (observed post-push): run **34660477874**
  on **e7ec0d4** = **SUCCESS** — all 13 jobs green (workflows-lint,
  lint, scripts-lint, unit, integration, e2e, real-HTTP e2e,
  arch-smoke, hermetic-smoke, windows-smoke, live-llm-gate,
  gitleaks; live-LLM smoke skipped by design, no DEEPSEEK_API_KEY).
- Observation loop closed for this delivery.
