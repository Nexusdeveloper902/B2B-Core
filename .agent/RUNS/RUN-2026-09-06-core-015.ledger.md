# RUN-2026-09-06-core-015 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Research sources (owner directive: "looking online")

Web searches saved as sandbox JSON (not repo files):
scripts/research-dash-ux.json, research-feed-ux.json,
research-polish.json, research-mobile.json, research-login.json,
research-nng.json — key sources: thoughtspot.com, resolution.de,
context.dev, datacamp.com, uxpilot.ai, pencilandpaper.io,
smashingmagazine.com (2025 real-time UX), fuselabcreative.com,
uxmatters.com, getstream.io, uxpatterns.dev, nngroup.com (empty
states, form errors, F-pattern), baymard.com, tetralogical.com,
querio.ai, toptal.com. Deep-read attempts via CLI web-reader failed
(function not available in this sandbox's z-ai CLI) — snippet-level
principles were sufficient and are quoted in the RUN record.

## Repository inspection

- Worklog + git state first: main @ 634ca57 (TASK-016 merged), tree
  clean; STATE queued TASK-017 as "planned and accepted"
- Current UI inventory: layouts/app, 7 views, 5 components,
  tokens.css (54 lines) + app.css (585 lines), realtime.js,
  lang EN/ES, DashboardTest/AdminPairingDeskTest pins (text + ids,
  not detailed markup → redesign freedom)
- tokens.css header warned of ADR-013 marketplace value-match →
  supersession decision recorded in ADR-027 before touching values

## State-modifying commands

- git switch -c feature/TASK-017-ux-overhaul
- File edits: public/css/tokens.css (rewrite + tokens),
  public/css/app.css (full rewrite, JS-facing selectors kept 1:1),
  public/js/realtime.js (rowFor/relative-time ticker; protocol
  contract untouched), layouts/app.blade.php (user chip),
  auth/login.blade.php (band + chips + reveal + script),
  partials/live-feed.blade.php (avatar/chip rows + rel strings),
  admin/dashboard.blade.php (KPI icons + loading states),
  teacher/dashboard.blade.php (summary chips + data-labels),
  admin/pairing.blade.php (countdown bar + busy states + stacks),
  parent/timeline.blade.php (chips + stacks),
  components/stat.blade.php (icon slot),
  components/empty.blade.php (icon slot),
  lang/en/app.php + lang/es/app.php (+12 keys each),
  AdminPairingController.php (pairingWindowSeconds view-data)
- File creates: .agent/DECISIONS/ADR-027-calm-ledger-ux.md,
  .agent/TASKS/TASK-017-ux-overhaul.md, .agent/RUNS/
  RUN-2026-09-06-core-015.md (+this ledger), STATE snapshot
- Test edits: tests/Feature/Web/DashboardTest.php (+6),
  tests/Feature/Web/AdminPairingDeskTest.php (+2) — zero existing
  assertions changed
- Gates: ./run test 211→219 (+8) → then Pint touch-target CSS fix
  (app.css @media 940px nav padding) → full re-run 219/3, quality
  PASS, e2e 24/24
- Browser proofs (raw php -S + realtime:serve; sandbox wrapper
  pattern): login micro-interactions, admin/teacher/pairing/parent,
  live tap (avatar MG + chip + "just now" + row-in), live row flip,
  countdown 96%→0%→red→expiry-hidden, NL is-loading, mobile 390 px
  card-stack + 44 px targets, reduced-motion emulation, Spanish;
  screenshots to /home/z/my-project/scripts/t17-*.png (sandbox
  artifacts); agent-browser errors empty on every page
- Bench hiccups caught + worked around: dev DB reseed had rotated
  reader API keys and card UIDs (first taps 401/404 until re-read);
  `set media reduced` is an invalid emulation keyword (silent
  no-op) — the valid value is `reduced-motion`
- Live taps through the device endpoint: event ids 1, 2 (Maria
  Late 19:56, Carlos Late 19:56) + one pairing arm→expire window
  (consumed nothing — expiry only)
- PROJECT.md facts append; TASK-017 acceptance boxes checked
- git add + commit + switch main + merge --no-ff (explicit ref) +
  push (main + feature branch) — hashes in the worklog Stage Summary
- Post-merge: ./run reset --force (bench rows from the verification
  replaced by the standard demo state; NOTE: reseed rotates reader
  keys/card UIDs — next bench must re-read them)
