# RUN-2026-09-02-core-001 — Activity Ledger

Forensic summary of significant operations (no secret values recorded).

- Repository investigation: cloned B2B-Core (with supplied PAT; remote
  immediately sanitized to the clean URL after clone). Finding: repository
  completely empty — zero commits, no .agent/, no code. First-run baseline:
  unborn `main`, clean tree.
- Environment: no system PHP/Composer, no root. Installed static PHP 8.4.23
  (static-php-cli "common" bundle) + Composer 2.10.3 under project tools
  (OBS-001). Network verified (github/packagist/static-php reachable).
- `composer create-project laravel/laravel` → Laravel 13.30.1 skeleton;
  default test suite green (baseline). `php artisan install:api` (Sanctum +
  routes/api.php) + `laravel/pint` (dev).
- Phase A: 9 migrations, 8 models, 5 enums, config/presence + config/recycling,
  DemoSeeder with bilingual credential printing. Fixed model↔table conventions
  (SchoolClass→classes, PresenceEvent→events, PointsLedger→points_ledger).
- Phase B: tap endpoint live-verified over HTTP (ok/401/404/ES-message/recycling
  next_step). Reader mode endpoint verified (admin ok, teacher 403, guest 401,
  invalid 422). Session login flow fixed (SetWebLocale must run AFTER
  StartSession — middleware appended, not prepended).
- Phase C: classify live-verified. Fixed missing import (MaterialClass) and
  deposit insert (points_awarded now part of the insert). Idempotency verified
  (retry returns already_classified, exactly one ledger row).
- Phase D: redemption verified (ok + 422 shortfall, EN/ES messages; teacher
  allowed, guest 401).
- Phase E: one live Gemini call attempted → HTTP 400 geo-restriction
  ("User location is not supported for the API use") from this environment
  (OBS-002). Endpoint honest-blocker path verified instead; full function-
  calling orchestration covered by mocked-transport unit tests.
- Refactor: MaterialClassifier binding moved into the container
  (AppServiceProvider) so tests can swap the contract.
- Testing: built 88 tests across Unit/Feature/E2E suites + real-HTTP
  scripts/e2e.sh (22 bilingual checks). Notable test-bug discoveries fixed:
  controller caching on Route objects (OBS-003 — mid-test swaps need
  mutable fakes bound before the first request); a secrets test initially
  embedded literal key values and was rewritten to credential PATTERN
  matching (never commit the values themselves); composer.lock content-hash
  false-positived a 32-hex pattern (pattern dropped).
- e2e.sh debugging: `php artisan serve` strips host env vars when .env exists
  (passthrough whitelist) — fixed with --no-reload (OBS-001); JSON bodies
  missing quotes around shell variables; material extraction regex captured
  multiple tokens.
- Lint: Pint auto-fix pass applied; final --test clean (107 files).
- Git: feature branch feature/TASK-002-core-platform-mvp with phase-tagged
  commits (see task file for the full commit log); history restructured once
  early (soft reset) to phase-slice properly; two file-restoration misses
  (routes/api.php, AppServiceProvider) were caught by the test suite and
  fixed before finalization. main initialized at branch tip e463611 (repo had
  zero prior commits) and pushed along with the feature branch.
- Main verification (fresh clone in a separate directory): composer install →
  cp .env.example .env → key:generate → migrate --seed (credentials printed)
  → php artisan test (88 pass / 1 skipped / 657 assertions) → scripts/e2e.sh
  (22/22) → live tap via curl (EN + ES). All green.
- Secret hygiene: .env (with the supplied keys) never committed (gitignored);
  remote URL sanitized after clone; push used a per-invocation credential URL;
  committed files scanned by pattern (test) and gitleaks (CI).
