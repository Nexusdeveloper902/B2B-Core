# RUN-2026-09-12-core-032 — TASK-032: UI controls & spacing passover

Owner directive (chat, 2026-09-12): whole-UI pass — inconsistent
spacing everywhere, text boxes + dropdowns not styled like the
alerts/pop-ups ("change those and keep the same style"), docs per
repo convention (docs/ + .agent/).

## Investigate-first

- Read the full control/alert CSS (`public/css/app.css` forms,
  searchbox, file-row, nl-answer, notice, field-error blocks),
  every view carrying `<input>`/`<select>` (all desks, both auth
  pages, teacher NL box, filterbars), and the pinning tests
  (DashboardTest CSS pins, AdminPairingDeskTest full-bleed grammar
  pin — that regex constrains WHERE new answer-margin rules may sit).
- Tree: main @ ab4f817 (TASK-031 merge), clean.

## Measurements that drove the fix

- 4 control surfaces, not 1: `.field` (44px/10+16/tinted? no —
  lowest/bordered) vs `.bare-*` (38px/7+12/lowest/bordered) vs
  `.searchbox` (tinted fill, TRANSPARENT border, own focus rule) vs
  file input (own copy + duplicated `max-width`). Selects: zero
  chevron styling (native arrows). Checkbox (`check-line`), `hr.rule`,
  pairing idle `.live-empty`: NO rules at all (unstyled, not
  mis-styled). `textarea`: focus rule, no box rule.
- 13 layout `style="…"` attributes in 7 views; 1 data-driven
  (`meter-fill` width %) correctly inline.
- Alerts (`.nl-answer`/`.notice` tones/padding/borders): the
  reference — untouched by design.

## Changes

- `public/css/app.css`: one shared surface selector (field + bare +
  search + file + textarea); select chevron; placeholder/disabled/
  invalid unification; `.check-line`, `.rule`, standalone
  `.live-empty`, 7 view-extracted utilities; `.ledger-wrap +
  .nl-answer` gap (placed BEFORE the pinned `.live-panel .nl-answer`
  override so it keeps winning); reward-grid `sp-md`; notices var
  fix. Zero alert restyling.
- 7 views: 13 inline styles → classes (meter width stays).
- `tests/Feature/Web/DashboardTest.php`: +1 pin (surface regex,
  chevron, new rules, untouched answer tones, desk HTML checks).
- Docs: `docs/UI-PASSOVER-2026-09-12.md` + `.es.md` (UI-AUDIT
  format); this ledger; TASK-032; PROJECT.md facts.

## Gates

- `./run test` → **443 passed / 3 skipped / 8 249 assertions**
  (baseline main 442/3 — delta is exactly the new pin)
- `./run quality` → PASS (Pint incl. the new test, shellcheck,
  docs parity)
- No rendered-browser pass this run (CSS-only change inside pinned
  grammar; full-bleed pin green) — noted honestly.

## Follow-up — "still looks the same" (same day)

- CSS verified correct on disk; live browser showed the STALE file
  (unversioned `css/app.css` URL → heuristic cache, no revalidation).
- Fix: `?v=filemtime` on the four head assets (layout only) +
  DashboardTest version pins. Proof: served `app.css?v=1789176949`,
  computed-style dump (select/input/answer/search identical
  surface), students-desk screenshot (chevrons, bordered search,
  styled checkbox + hrs). Artifacts removed; server stopped.
- Ops lesson: `pkill -f` self-matches — PID inspection first.

## Status
DONE 2026-09-12 — uncommitted tree; commit/push per owner call.
Follow-up recorded (not started): Datum-styled replacement for
native `confirm()` on unpair/key-rotation (product/a11y call).
