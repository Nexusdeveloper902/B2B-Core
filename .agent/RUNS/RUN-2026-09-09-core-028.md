# RUN-2026-09-09-core-028 — TASK-029: the realtime passover, class creation & GUI completion

## Context
Owner's passover report (chat, 2026-09-09): class creation missing,
grade typing (with °) annoying, students desk not live, teacher class
panel not live, "everything that could change needs websockets", and a
UI-consistency sweep. main was green at 7b2fa23 (RUN-027).

## Changes
- **ADR-042 — the roster channel**: fourth WS channel on the same
  wire. Append-only `roster_updates` (migration 2026_09_09_000001),
  `RosterUpdate` model, `RealtimeRoster` (read) + `RosterUpdateLog`
  (write); frames `student_created` / `students_imported` (one per
  import) / `class_created` / `reader_updated`, written inside the
  same transaction as the change by StudentController (store+import),
  ClassController, ReaderSettingsController AND ReaderModeController;
  `realtime:serve` polls + broadcasts to ADMIN connections only; the
  admin hello carries the recent roster snapshot (idempotent replay).
- **Class creation**: `POST /api/v1/admin/classes` +
  `ClassStoreRequest` + route; duplicate = bilingual 422
  (case-insensitive); optional teacher assignment (role-checked);
  form in the students desk (name + teacher select, live select
  updates via roster frames).
- **Grade select** 0°–11° (replaces the typed-text input).
- **realtime.js**: `realtime:roster` CustomEvent dispatch + hello
  roster snapshot replay.
- **Passover (view layer)**: students desk live roster (idempotent
  prepend); readers desk live label/mode (focus-guarded); teacher
  dashboard chips + KPI live (bump logic, first-tap-wins); admin
  dashboard KPI live (distinct-student Sets) + readers table live;
  parent timeline live prepend (viewed student's taps; pills/search
  own visibility); student history live ledger (true running balance
  from frames); student leaderboard live re-sort (server's rule:
  points DESC, student_id ASC) + rank/balance stats; student
  dashboard balance — FIXED latent bug (listener read frame.payload;
  server sends frame.update.payload) + stat hook.
- **UI consistency**: design-system paginator
  (resources/views/vendor/pagination/tailwind.blade.php override;
  .pagination CSS already existed); `.file-row` file-input styling;
  `.ta-right` utility replacing scattered inline right-aligns
  (timeline, ecostation, teacher filterbar inline flex → CSS).
- **i18n**: 9 new app keys + 2 api keys, en+es parity.
- **Docs**: API.md/.es.md (classes endpoint + roster frames),
  FRONTEND.md/.es.md (§3b passover coverage table + honest limits),
  ARCHITECTURE/realtime-feed.md (roster channel), ADR-042.

## Verification (local)
- `./run test` → **330 passed / 1 skipped** (RUN-027: 314; +16: 5
  ClassManagement, 1 RealtimeServer roster, 2 StudentManagement
  roster assertions, 1 ReaderSettings transaction, 9
  RealtimePassover), 4,918 assertions.
- `./run quality` → PASS · `./run e2e` → **33/33**.
- Real-socket proof: roster frames reach admin connections only
  (teacher connection stays silent — readMessageIfAny timeout proof).

## Status
DONE locally — push + remote CI observation below.

## Push outcome
- PAT via per-command credential helper only; never in any tree,
  URL, command line, or record.
- Remote CI verdict: appended below after observing the runs this
  push triggers (trap rule: observe the run your own record commit
  triggers too).
