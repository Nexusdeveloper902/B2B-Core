# RUN-2026-09-15-core-044 — TASK-043 mDNS advertisement (backend half)

## What was done
Backend half of the hard-coded-IP removal, in dependency order:
firmware investigation first (8 findings: single `API_BASE_URL` value,
two consumption points, API `:8000` vs dashboard-only WS `:8081`, no WS
client / no mDNS / no NVS in firmware, ESPmDNS in-core, existing
`WifiService` + `EspApiClient` seam), then `scripts/serve.sh`
(`start_mdns()` + `B2B_MDNS` + cleanup + `Found:` line), then
`scripts/mdns/pulse.service`, `docs/MDNS.md` (+`.es.md`),
`docs/SCRIPTS.md` (+`.es.md` serve paragraph), ADR-061, TASK-043 file.
Firmware half (B2B-Firmware TASK-013, ADR-015) built and verified first;
the contract (`_pulse._tcp`, port = serve port, TXT
`version=1 protocol=1 api=/api`) is byte-identical on both sides.

## Gates
- `./run quality`: PASSED (bash -n 16 files, Pint clean, docs parity
  serve EN+ES green; shellcheck skipped locally as usual — CI enforces).
- Live DNS-SD round-trip (python `zeroconf`, loopback): registered
  `Pulse._pulse._tcp.local` 192.168.43.91:8000 with the exact TXT set,
  browsed + resolved + TXT-verified — CONTRACT OK. (This box runs no
  avahi-daemon and grants no sudo, so `avahi-publish-service` itself is
  CLI-syntax-verified only; it publishes byte-identical records.)
- Firmware gates (other repo, same session): native 118/118 (+6),
  `esp32dev` / `esp32dev-mock` / `esp32cam-reader` / `esp32cam` SUCCESS.

## Traps
- `write` tool variant fails with arg errors in this environment;
  `filesystem_write_file` / `filesystem_edit_file` are the working pair.
- `CapturePayload::contentType()` returns `std::string` — the station
  `post()` wrapper takes `const std::string&`, not `const char*`.
- `discoverPulseServer()` in `src/main.cpp` needs a forward declaration
  (defined below `setup()`).

## Status
DONE 2026-09-15 — code + docs + memory in place; bench rotation test
pending (no hardware in agent runs; procedure in `docs/MDNS.md`).
