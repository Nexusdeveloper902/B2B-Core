# RUN-2026-09-04-core-005 — Gemini docs pull + error taxonomy + llm-check (TASK-007)

Run ID: RUN-2026-09-04-core-005
Date: 2026-09-04
Task: TASK-007-gemini-docs-error-taxonomy
Branch: main only
Protocol: stateless agent master execution protocol (repo reality
authoritative, append-only memory, main must remain buildable).

## Owner request

> "Using the same stateless protocol, please pull the actual gemini api
> docs bc it still aint working"

## What was delivered

**Verification against the real Google docs + failure self-explanation.**

1. Docs pulled live (web-reader): generateContent REST reference, API
   errors contract, function-calling guide, troubleshooting guide,
   available-regions. Key truths: documented error body
   `{code, message, status, details[].reason}`; invalid key = 400
   API_KEY_INVALID; region refusal = 400 FAILED_PRECONDITION (no
   reason); Colombia IS supported; generateContent labeled Legacy
   (Interactions API is the future — OBS-008); TASK-006 wire format
   confirmed correct.
2. Differential live tests: old AIza key → region error (STILL VALID —
   "dead key" was wrong); fake AIza key → API_KEY_INVALID; fake AQ.
   key → 401. Each failure class observed, not guessed.
3. Error taxonomy (ADR-016): typed exceptions in GeminiClient, distinct
   blocked_reasons + bilingual actionable messages in the API, raw cause
   logged. `GeminiClientTest` (8 tests) with verbatim doc fixtures.
4. **`./run llm-check`** — one-command local self-diagnosis (bash+curl,
   no PHP): prints Google's exact verdict for the machine it runs on,
   EN/ES guidance, exit 0/1/2. Live-verified in both the region and
   invalid-key branches; fixed a live-found `set -e` trap (unmatched
   grep in `$(…)` killed the script pre-diagnosis).
5. Docs: API.md/.es blocker table, SCRIPTS.md/.es llm-check sections,
   README hints — bilingual. 119 local tests, 2320 assertions.

## Commit chain on main

- `b526db2` feat(llm): docs-verified error taxonomy + ./run llm-check
  self-diagnosis
- `9287391` fix(scripts): mark llm-check.sh executable in the git index
  (OBS-009: core.fileMode=false hides +x on new files — 3 CI jobs
  caught it locally-green tests missed)

## CI proof

- Run 33811543860 (9287391): **12/12 jobs success**; live-LLM smoke
  passes with 7 tests (incl. the 2 new blocker tests) and probe
  HTTP 200 on gemini-3.1-flash-lite.
- Intermediate failure 33811411626 (b526db2) was the fileMode trap —
  honest red, fixed, green.

## Secret hygiene

Full-diff scan for AIza/AQ./ghp patterns: 0 matches. llm-check prints
key prefix + length only, never the value. New key remains only the
Actions secret.

## The answer to "it still ain't working"

The pipeline itself works (CI live round-trip green, docs-verified wire
format). Local failures were UNDIAGNOSABLE by design (one generic
"unavailable" for every cause). Now: run `./run llm-check` on the
affected machine — it prints Google's verdict for THAT machine and the
fix. Likely local causes (now distinguishable): stale .env key/model
(setup never overwrites .env) or egress-region refusal (Colombia is
supported; VPN/proxy/ISP routing matters).
