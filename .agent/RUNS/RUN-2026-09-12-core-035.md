# RUN-2026-09-12-core-035 — TASK-035: readers table off the admin dashboard

Owner (chat, 2026-09-12): remove the readers part from the admin
dashboard.

## Changes
- View: readers panel + mode JS + roster repaint listener gone; NL
  panel full width; comments honest.
- Controller: dead `readers` query gone. CSS: dead `.mode-form` /
  `.mode-select` rules + inventory comment gone.
- Lang subtitle EN+ES. Tests renamed + repointed (3 files).
- Docs: FRONTEND.md + .es.md coverage row + readers section.

## Gates
- `./run test` **445 passed / 3 skipped** · `./run quality` PASS.
- One red during the work (over-broad `assertDontSee` on reader
  names — feed contexts legitimately show them); narrowed to the
  removed artifacts. Lesson recorded in TASK-035.

## Follow-up — NL panel spacing (same day)

- Bare section touched its neighbors (0px vs 16px). New
  `.section-gap` utility; measured 16px joints + shot. Pin added.

## Status
DONE 2026-09-12 — uncommitted tree; commit/push per owner call.
