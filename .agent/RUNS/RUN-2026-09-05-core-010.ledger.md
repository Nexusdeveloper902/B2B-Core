# RUN-2026-09-05-core-010 — activity ledger

Repository/state-modifying operations only (no trivial commands, no
secret values).

## Repository inspection
- Worklog TASK-007/TASK-011 entries (bench context: pairing desk armed
  from desktop OK; phone 401; device pair taps 401)
- B2B-Core: routes/api.php (admin group auth:sanctum + role),
  bootstrap/app.php ($middleware->statefulApi()),
  config/sanctum.php (stateful default + commented placeholder),
  resources/views/admin/pairing.blade.php (desk JS: X-CSRF-TOKEN +
  credentials same-origin; NO manual pair form — the two
  /api/v1/admin/cards/pair calls in the owner's serve log were the
  device's taps), lang/en+es api.php,
  vendor/laravel/sanctum/src/{Guard.php, Sanctum.php,
  SanctumServiceProvider.php, Http/Middleware/
  EnsureFrontendRequestsAreStateful.php} + framework Middleware.php
  (statefulApi wiring), phpunit.xml (no SANCTUM_STATEFUL_DOMAINS),
  .env.example, scripts/serve.sh (--host support),
  tests/Unit/DocumentationTest.php + Feature/Api/PairingStatusTest.php
  (style), .agent PROJECT.md + TASKS numbering (next = TASK-012)
- B2B-Firmware: EspApiClient.h, CoreTypes.h, secrets.h.example,
  platformio.ini, arduino-esp32 HTTPClient source in the pio cache
  (setAuthorization → _base64Authorization with default type "Basic")

## State-modifying commands
- git checkout -b feature/TASK-012-stateful-lan-access
- File edits: config/sanctum.php, .env.example,
  tests/Unit/StatefulDomainMatchingTest.php (new),
  tests/Unit/DocumentationTest.php, docs/API.md, docs/API.es.md,
  .agent/{DECISIONS/ADR-022-request-host-stateful.md,
  TASKS/TASK-012-stateful-lan-access.md,
  RUNS/RUN-2026-09-05-core-010.md(+ledger), STATE snapshot, PROJECT.md}
- B2B_PHP=/home/z/my-project/tools/php ./run test (x2 — after Pint fix)
- B2B_PHP=/home/z/my-project/tools/php ./run quality (x2)
- B2B_PHP=/home/z/my-project/tools/php ./run e2e
- git add + commit + checkout main + merge --no-ff + push (main +
  feature branch) — see RUN file Verification section
