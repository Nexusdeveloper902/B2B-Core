# OBS-004 — laravel/boost not installed for the ops-scripts run

## Date
2026-09-03

## Observation
The scaffold's `AGENTS.md` instructs agents to `composer require laravel/boost`
+ `php artisan boost:install` "before making application changes". RUN-001
did not install it either (absent from composer.lock as of edbc48a).

## Decision for RUN-2026-09-03-core-002
Not installed, deliberately:
- This run touches **zero application code** (ADR-009 scope: operational
  scripts, docs, CI, tests-only additions). Protocol §1.4 (minimize unrelated
  changes) applies.
- `boost:install` rewrites `AGENTS.md` and adds dev dependencies to
  composer.json/lock — unrelated churn on a verified buildable main.

## Follow-up
A future run that modifies application code should evaluate installing
laravel/boost and letting it regenerate AGENTS.md, then supersede this
observation (append-only — do not edit this file).
